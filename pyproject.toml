[project]
name = "slopbox"
version = "0.1.0"
description = "A secure-ish environment for running AI agents."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "click>=8.4.2",
    "pydantic>=2.13.4",
]

[dependency-groups]
dev = [
    "ruff>=0.15.21",
]
